#include <Arduino.h>

#define SERIAL_BITS_PER_SECOND 115200

void setup() {
  // put your setup code here, to run once:
  Serial.begin(SERIAL_BITS_PER_SECOND);
}

void loop() {
  // put your main code here, to run repeatedly:
  
}